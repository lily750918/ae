# AE Server — Python 依赖（安装：pip install -r requirements.txt）
# 版本为兼容区间，部署前可用 pip freeze 固定具体版本

Flask>=3.0,<4
flask-cors>=4.0,<6
Flask-HTTPAuth>=4.8,<6
requests>=2.28,<3
python-binance>=1.0.19
Werkzeug>=3.0,<4
